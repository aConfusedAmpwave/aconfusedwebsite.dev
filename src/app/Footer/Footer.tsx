import { useState } from "react";

//Tabs(h3) for Home, About, Projects, and Contact with sublinks(h4) to categories in them. Friends list on the side with the 88x31 for each of them.
export default function Footer () {
    return <>
    </>
}